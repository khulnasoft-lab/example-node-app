name: Test node with worklows
on: push
jobs:
  first-job:
    name: Build with Node
    runs-on: ubuntu-latest
    container: node:21.5.0
    steps:
      - run: node --version
      - run: npm --version
